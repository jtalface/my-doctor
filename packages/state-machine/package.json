{
  "name": "@mydoctor/state-machine",
  "version": "2.0.0",
  "description": "State machine v2 for MyDoctor - with enhanced modules, analytics, screening logic, risk scores, and multilingual support",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": ["mydoctor", "state-machine", "health", "wellness"],
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}

