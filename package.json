{
  "name": "mydoctor-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "MyDoctor monorepo - AI-powered health assistant",
  "scripts": {
    "dev": "turbo run dev --filter=@mydoctor/app",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "keywords": ["mydoctor", "monorepo", "health", "ai"],
  "license": "ISC",
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
